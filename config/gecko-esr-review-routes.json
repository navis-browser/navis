{
  "schema_version": 1,
  "description": "Cross-cutting review routes for every path changed between two pinned Gecko ESR trees. A path can match multiple routes; unmatched paths remain in the complete general-upstream inventory.",
  "default_route": "general-upstream",
  "routes": [
    {
      "id": "build-configuration-and-packaging",
      "description": "Configure, mozbuild, Rust workspace, and runtime packaging behavior that can change the selected Desktop Embedder graph.",
      "patterns": [
        "^(?:build|config|python/mozbuild)(?:/|$)",
        "^(?:moz|toolkit/moz)\\.configure$",
        "^toolkit/(?:toolkit\\.mozbuild|mozapps/installer)(?:/|$)",
        "^Cargo\\.(?:toml|lock)$"
      ]
    },
    {
      "id": "startup-process-and-sandbox",
      "description": "Parent/child startup, process bootstrap, IPC, launcher, mozglue, and native sandbox contracts.",
      "patterns": [
        "^(?:browser/app|ipc|mozglue|security/sandbox|toolkit/xre)(?:/|$)"
      ]
    },
    {
      "id": "platform-host-contracts",
      "description": "Desktop widget, accessibility, platform integration, and XPCOM host lifecycle behavior.",
      "patterns": [
        "^(?:accessible|toolkit/system|widget|xpcom/build)(?:/|$)"
      ]
    },
    {
      "id": "privileged-toolkit-contracts",
      "description": "Privileged modules and services directly consumed by Runtime, Session, View, or Delegate adapters.",
      "patterns": [
        "^toolkit/(?:actors|modules)(?:/|$)",
        "^toolkit/components/(?:bitsdownload|downloads|enterprisepolicies|passwordmgr|permissions|prompts|satchel|shell)(?:/|$)"
      ]
    },
    {
      "id": "fission-navigation-and-security",
      "description": "BrowsingContext/Fission, docshell, principal, navigation, history, and network behavior retained by every profile.",
      "patterns": [
        "^(?:caps|docshell|dom/(?:base|ipc|security)|netwerk|toolkit/components/places)(?:/|$)"
      ]
    },
    {
      "id": "web-platform-engine",
      "description": "The Gecko core closure that supplies general Web behavior rather than product presentation.",
      "patterns": [
        "^(?:dom|gfx|image|js|layout|media|modules/libpref|netwerk|storage|xpcom)(?:/|$)"
      ]
    },
    {
      "id": "optional-capability-candidates",
      "description": "Subsystems already identified as profile-dependent or requiring an explicit compatibility decision.",
      "patterns": [
        "^(?:dom/(?:ai|media/(?:webrtc|webrtc-js)|ml|webgpu)|gfx/(?:vr|wgpu_bindings)|media/webrtc|toolkit/components/(?:extensions|glean|ml|telemetry)|toolkit/mozapps/extensions)(?:/|$)",
        "^third_party/(?:libwebrtc|onnxruntime|rust/(?:glean|wgpu)[^/]*)(?:/|$)"
      ]
    },
    {
      "id": "automation-and-debugging",
      "description": "WebDriver BiDi, Marionette, Remote Agent and future DevTools changes that can alter automation reach, privileged access or extension mutation surfaces.",
      "patterns": [
        "^(?:remote|testing/(?:firefox-ui|marionette))(?:/|$)"
      ]
    },
    {
      "id": "third-party-and-rust-supply-chain",
      "description": "Vendored native/Rust code and dependency-lock changes requiring licensing, security, and graph review.",
      "patterns": [
        "^(?:third_party|toolkit/library/rust)(?:/|$)",
        "^Cargo\\.(?:toml|lock)$"
      ]
    }
  ]
}
